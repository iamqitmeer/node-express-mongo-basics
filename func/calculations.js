const sumOfTwoNum = function (a, b) {
  console.log(`Sum of ${a} & ${b} is -- ${a + b}`);
};

const mulOfTwoNum = function (a, b) {
  console.log(`Sum of ${a} & ${b} is -- ${a * b}`);
};

export { sumOfTwoNum,mulOfTwoNum };
